// For the two cards whose result is a rendered artifact, the diff and the pull
// request. Everything else a tool produces is a row, so this stays small.

export const TOOL_CARD_CLASS = "gap-3 border border-border py-3 ring-0";
